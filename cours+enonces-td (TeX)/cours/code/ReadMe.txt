BEST: Command to copy a Python code into a rtf file with highlighted syntax:

pygmentize -P py3 -o file.rtf file.py


( 

OTHER:

Command to copy a Python code into a rtf file with highlighted syntax:

highlight -O rtf myfile.py | pbcopy

And then paste it in a test rtf file.

)